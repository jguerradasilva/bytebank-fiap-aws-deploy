import { Box, Typography } from '@mui/material';

export default function PageTransferir() {
  return (
    <Box sx={{ color: '#fff' }}>
      <Typography variant="h4">Transferência - Em Desenvolvimento</Typography>
    </Box>
  );
}
